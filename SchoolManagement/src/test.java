import javax.swing.JPanel;

public class test extends JPanel {

	/**
	 * Create the panel.
	 */
	public test() {

	}


/*
 while (login is correct)
	if(admin button is selected and login is found in admin table) {
		open admin class
	} else {
		show incorrect radio button
	}

	if(student button is selected and login is found in student table) {
		open Student class
	} else {
		show incorrect radio button
	}
	
	if(if teacher button is selected and login is found in teacher table) {
		open teacher class
	} else {
		show incorrect radio button
	}
*/	
}